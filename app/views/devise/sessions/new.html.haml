.LoginBoard

  .LoginBoard__HaveNoAccount
    %p.LoginBoard__HaveNoAccount__Text
      アカウントをお持ちでない方はこちら
    = link_to new_user_registration_path, class: "LoginBoard__HaveNoAccount__PinkBtn" do
      %p.LoginBoard__HaveNoAccount__PinkBtn__Text
        新規会員登録

  .LoginBoard__InputBox
    = form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: 'LoginBoard__InputBox__Form'}) do |f|
      .LoginBoard__InputBox__Form__Email
        = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: " メールアドレス"
        %br/
        %p (例) example@gmail.com
      .LoginBoard__InputBox__Form__Pass
        = f.password_field :password, autocomplete: "current-password", placeholder: " パスワード"
        %br/
        %p ※7字以上、半角英数字のみ
      - if devise_mapping.rememberable?
        .LoginBoard__InputBox__Form__Check
          = f.check_box :remember_me
          = f.label :remember_me
      .LoginBoard__InputBox__Form__BlueBtn
        = f.submit "ログイン", class: "LoginBoard__InputBox__Form__BlueBtn__BlueLoginBtn"

    - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
      .LoginBoard__InputBox__ForgotPass
        = link_to "パスワードをお忘れの方", new_password_path(resource_name), class: "LoginBoard__InputBox__ForgotPass__Text"
